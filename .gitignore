/target

# Local-only engine path override (developer machines only; see README).
/.cargo/config.toml

# Raw multi-agent research dossiers: kept locally for reference, not published
# (they contain absolute local paths and working notes). The distilled, public
# version lives in docs/DESIGN.md.
/docs/research/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# oh-my-claudecode session state (local only)
.omc/

# Local-only working docs — never commit (private direction / coordination)
docs/GOAL.md
docs/COORDINATE.md
docs/DEFERRED-QUESTIONS.md
